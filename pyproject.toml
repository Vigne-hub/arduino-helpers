[project]
name = "arduino-helpers"
version = "0.7.0"
description = "arduino-helpers"
readme = "README.md"
requires-python = ">=3.9"
license = "BSD-3-Clause"
license-files = ["COPYING"]

[project.urls]
Homepage = "https://github.com/Vigne-hub/arduino-helpers"

[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
# Package data is selected by MANIFEST.in.
include-package-data = true

[tool.setuptools.packages.find]
include = ["arduino_helpers*"]

# --- pixi -----------------------------------------------------------------
# `pixi build` builds the noarch conda package with the pixi-build-python
# backend; conda run dependencies live in [tool.pixi.package.run-dependencies].
[tool.pixi.workspace]
channels = ["conda-forge", "alexsk", "vignesh229", "https://prefix.dev/vigne-hub/scibots"]
platforms = ["win-64", "linux-64"]
preview = ["pixi-build"]

[tool.pixi.package]
name = "arduino-helpers"
version = "0.7.0"

[tool.pixi.package.build.backend]
name = "pixi-build-python"
version = "0.*"
channels = ["https://prefix.dev/conda-forge"]

[tool.pixi.package.host-dependencies]
setuptools = ">=77"

[tool.pixi.package.run-dependencies]
"numpy" = "*"
"pandas" = "*"
"path-helpers" = "*"
"serial-device" = "*"

# --- commitizen -----------------------------------------------------------
# `cz bump` derives the next version from Conventional Commits, stamps every
# copy of the version below, updates CHANGELOG.md and tags vX.Y.Z.
[tool.commitizen]
name = "cz_conventional_commits"
version_provider = "pep621"
tag_format = "v$version"
bump_message = "chore: release v$new_version [skip ci]"
update_changelog_on_bump = true
version_files = [
    "pyproject.toml:^version",
    "arduino_helpers/_version.py:^__version__",
]
